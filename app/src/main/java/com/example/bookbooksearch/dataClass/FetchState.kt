package com.example.bookbooksearch.dataClass

enum class FetchState {
    HttpException, UnknownHostException, IllegalStateException, FAIL
}

/*data class FetchState(
    val httpStatusCode: Int,
    val message: String,
    val errorCode: String?
)*/
